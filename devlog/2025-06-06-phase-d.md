# Phase D 开发日志 — 2025-06-06

## 完成了什么
- 图片轮播：支持多图横滑，左右按钮 + 圆点指示 + 触屏滑动
- frontmatter 支持 `images:` 字段（逗号分隔多图路径），兼容旧的 `image:`
- 单图时正常显示，多图时自动切换为轮播模式
- 入场动画：区块淡入上移（Intersection Observer）
- 时间线节点 stagger 逐个入场（每个延迟 120ms）
- 项目卡片 stagger 逐个入场（每个延迟 100ms）
- 卡片 hover 上浮效果（CSS transition）
- 脚本加载顺序修正：animations.js 暴露全局函数，markdown-loader 渲染后调用

## 待办
- 部署到 GitHub Pages
- 填充真实图片和项目描述

## 遇到的问题
- animations.js 初始化时机问题：timeline/project 内容是异步加载的，需要渲染完成后手动触发动画初始化。已通过暴露全局函数解决
