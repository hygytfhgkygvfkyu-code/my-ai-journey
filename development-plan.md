# 分阶段开发计划

## 总览

| 阶段 | 目标 | 任务数 | 产出 |
|------|------|--------|------|
| A | 骨架搭建 | 6 | 可打开的空白页面 + 导航可用 |
| B | 内容区块 | 5 | 所有区块结构就位，内容可见 |
| C | 数据接入 | 5 | Markdown 渲染 + 项目卡片完整呈现 |
| D | 动效打磨 | 6 | 完整网站，可部署上线 |

---

## 阶段 A：骨架搭建

**目标**：HTML 结构 + CSS 基础 + 导航栏，页面可以滚动定位

### 任务
- **A1** 创建 `index.html`，写入完整 HTML 骨架（7个区块的容器）
- **A2** 创建 `css/style.css`，写入 CSS Variables + reset + 基础排版样式
- **A3** 实现导航栏：固定顶部、滚动＞60px 变白底、导航链接
- **A4** 创建 `js/nav.js`：点击导航链接平滑滚动到对应区块 + 滚动高亮当前项
- **A5** 移动端汉堡菜单
- **A6** 创建 `js/data.js`，写入 hero / skills / contact 占位数据

### 验收条件
- [ ] 浏览器打开 `index.html`，页面正常渲染
- [ ] 导航栏点击可平滑滚动到对应区块
- [ ] 滚动时当前区块对应的导航项高亮
- [ ] 手机宽度下出现汉堡菜单，点击可展开
- [ ] 无控制台报错

---

## 阶段 B：内容区块

**目标**：所有区块的 HTML + CSS 完成，占位内容填充

### 任务
- **B1** Hero 区块：头像圆图 + 姓名 + 简介 + 致敬语，min-height: 100vh
- **B2** About 区块：教育背景卡片 + 简洁自我介绍
- **B3** Skills 区块：技能标签云 + 熟练度显示
- **B4** AI Timeline 区块：纵向时间线 HTML 结构 + CSS 样式
- **B5** Contact / Footer 区块

### 验收条件
- [ ] 7个区块全部可见，样式完整
- [ ] 桌面端 960px 居中，移动端全宽
- [ ] 每个区块间距、排版符合 ui-design.md
- [ ] 时间线竖线+圆点渲染正确，移动端统一右侧
- [ ] 卡片阴影、圆角、颜色符合规范

---

## 阶段 C：数据接入

**目标**：Markdown 内容加载机制 + 所有项目数据上线

### 任务
- **C1** 创建 `js/markdown-loader.js`：用 marked.js 加载并渲染 .md 文件
- **C2** 创建 `content/projects/` 下 6 个项目 .md 文件，写入你的真实项目内容
- **C3** 创建 `content/timeline.md`，写入你的 AI 学习时间线数据
- **C4** 项目卡片区块：JS 动态读取 projects/ 目录并渲染卡片
- **C5** 时间线区块接入 `timeline.md` 数据

### 验收条件
- [ ] 6个项目 .md 的 HTML frontmatter 注释正确被解析
- [ ] 项目卡片渲染在页面上：图片占位+标题+简介+标签+成果
- [ ] 时间线渲染在页面上：节点逐个正确的顺序和样式
- [ ] 新增一个 .md 测试文件，卡片自动增加（无需改代码）

---

## 阶段 D：动效打磨 + 上线

**目标**：动效、占位图、响应式收尾、部署

### 任务
- **D1** 创建 `js/animations.js`：Intersection Observer 区块入场动画 + 时间线 stagger 入场
- **D2** 卡片 hover 效果：上浮 4px + 阴影加深
- **D3** 占位图片替换为真实素材（头像、项目截图）
- **D4** 响应式细节：完整走查移动端/桌面端效果
- **D5** 性能检查 + 最终文字校对
- **D6** 部署到 GitHub Pages

### 验收条件
- [ ] 滚动时区块依次淡入入场，时间线节点逐个出现
- [ ] 卡片 hover 有上浮效果
- [ ] 所有图片无拉伸/变形/缺失
- [ ] 移动端和桌面端浏览无视觉问题
- [ ] Lighthouse 性能评分 ≥ 90
- [ ] 线上 URL 可访问

---

## 阶段间规则

1. 每个阶段完成后**停下**，等你验收确认
2. 确认后 git commit，再进入下一阶段
3. 每次写代码后，在 `devlog/` 记录日志
4. Bug 修复流程：复现 → 分析根因 → 提方案 → 你批准 → 动手
